# Matmakefile
# See https://github.com/mls-m5/matmake2

main
  out = main
  src =
    src/*.cpp
    src/*.cppm
  copy = data/
  includes = include
  command = [exe]

all
  in = @main
  config =
    Wall
    Wpedantic
    modules
    c++17

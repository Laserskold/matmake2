# Matmake v2

matmake2
  out = matmake2
  src =
    src/*.cpp
    src/main/*.cpp
  includes =
    src
    lib/json.h/include
  command = [exe]
  ldflags = -pthread
  copy = demos

all
  in = @matmake2
  config =
    c++17
    Wall
    Wpedantic
    thread

